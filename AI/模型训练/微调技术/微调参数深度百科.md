---
title: LLM 微调参数深度百科：从底层逻辑到实战调优的全量字典
created_at: 2026-05-13
updated_at: 2026-05-13
tags: [LLM, 微调, 参数百科, LLaMA-Factory, 训练工程]
---

# LLM 微调参数深度百科

本手册旨在对大型语言模型（LLM）微调过程中涉及的每一个配置参数进行“细胞级”的拆解。无论是基础的 LoRA 设置，还是复杂的优化器调度，均提供底层物理意义解释、实战影响分析及推荐取值范围。

---

## 一、 LoRA (Low-Rank Adaptation) 核心参数族

### 1.1 LoRA 秩 (Rank, `r`)
- **底层原理**：LoRA 将权重更新 $\Delta W$ 分解为两个矩阵 $A$ 和 $B$ 的乘积（$A \in \mathbb{R}^{d \times r}, B \in \mathbb{R}^{r \times k}$）。`r` 即为这两个矩阵的共享维度。
- **物理意义**：它定义了模型“学习新知识的管道宽度”。
- **详尽影响**：
    - **低 Rank (4-8)**：捕捉浅层的指令风格。如果你的 7000 条数据准确率卡在 75%，说明这个通道太窄，模型无法学习到复杂的逻辑关联。
    - **高 Rank (64-256)**：能够承载深层的领域知识注入。随着 Rank 增加，可训练参数线性增长，拟合上限显著提升。
- **2026 建议**：复杂推理任务（数学、代码）首选 64 或 128。

### 1.2 LoRA 缩放系数 (Alpha, `lora_alpha`)
- **物理意义**：作为 $\Delta W$ 的比例因子。
- **公式作用**：$\text{Output} = Wx + \frac{\alpha}{r}(BA)x$。
- **深度解析**：$\alpha$ 的存在是为了在不同 `r` 下保持训练的数值稳定性。通常设定为 $2 \times r$。如果你修改了 `r` 但不修改 `alpha`，权重的实际影响力会发生非预期偏移。

### 1.3 目标模块 (Target Modules)
- **底层逻辑**：指定 LoRA 适配器挂载的具体线性层。
- **详尽列表**：
    - `q_proj`, `k_proj`, `v_proj`, `o_proj`：注意力机制。
    - `gate_proj`, `up_proj`, `down_proj`：MLP (Feed-Forward) 模块。
- **实战准则**：**all-linear** 是 2026 年的标配。只微调注意力层会导致模型在知识记忆上表现极差，必须覆盖所有线性层以达到类似全量微调的效果。

### 1.4 LoRA+ 学习率比例 (LoRA+ LR Ratio)
- **底层原理**：LoRA+ 论文提出，矩阵 $B$ 的学习率应该比矩阵 $A$ 大（通常是 2^4 倍）。
- **实战作用**：能显著加快 LoRA 的收敛速度，尤其在复杂任务中能减少约 30% 的训练时长。

### 1.5 LoRA 随机丢弃 (LoRA Dropout)
- **作用**：在训练过程中随机使一部分 LoRA 神经元失效。
- **推荐**：对于 7000 条这种中等规模数据，建议设为 **0**。只有在数据极少（<500条）担心过拟合时，才设置 0.05-0.1。

---

## 二、 训练策略与优化器参数

### 2.1 学习率 (Learning Rate, `lr`)
- **物理意义**：优化器在负梯度方向上更新权重的“跨度”。
- **实战分水岭**：
    - **LoRA**：推荐 $1 \times 10^{-4}$ 到 $2 \times 10^{-4}$。
    - **Full SFT**：推荐 $5 \times 10^{-6}$ 到 $2 \times 10^{-5}$。
- **注意**：学习率是微调的第一命门。Loss 曲线如果不下降，优先检查 LR 是否过低。

### 2.2 批处理大小 (Batch Size) 与 梯度累积 (Gradient Accumulation)
- **计算公式**：等效 Batch Size = $\text{Batch per device} \times \text{Accumulation Steps} \times \text{Device count}$。
- **深度解析**：大的等效 Batch Size 能提供更准确的梯度估计。
    - **16 以下**：梯度噪声大，容易在局部最优解反复横跳（你的 75% 现象可能与此有关）。
    - **64-128**：公认的黄金区间，兼顾收敛速度与平滑度。

### 2.3 学习率调节器 (Scheduler)
- **`cosine` (余弦下降)**：从初始 LR 平滑下降到 0。推荐用于最终性能追求。
- **`linear` (线性下降)**：匀速下降，适合初步实验。
- **`constant` (恒定)**：仅用于测试梯度。

### 2.4 最大梯度范数 (Max Grad Norm)
- **作用**：梯度裁剪（Gradient Clipping）。
- **深度解析**：如果计算出的梯度向量长度超过该值（通常设为 1.0），则将其强行按比例缩小。这是**防止模型在训练中途崩溃（Loss 变 NaN）**的关键防火墙。

---

## 三、 数据处理与对齐参数

### 3.1 对话模板 (Prompt Template)
- **底层逻辑**：定义了模型如何识别 System、User、Assistant 的边界。
- **致命细节**：Qwen 2.5 使用 ChatML。如果模板选错，模型会把控制符（如 `<|im_start|>`）当成普通文本去学，导致推理时无法正确触发回复逻辑。

### 3.2 截断长度 (Max Length / Cutoff Length)
- **作用**：单条数据最长 Token 数。
- **详尽后果**：如果你设置 2048，但数据有 3000 Token。
    - **损失**：被掐掉的不仅仅是文字，更是逻辑的完整性。
    - **推荐**：始终通过 `hist` 工具检查数据集长度分布，确保覆盖 95% 以上的样本。

### 3.3 NEFTune 噪声注入 (Noise Alpha)
- **原理**：在 Embedding 层注入噪声。
- **实战作用**：能有效防止模型过拟合到训练数据的字面表述上，显著提升模型的“指令泛化能力”。如果你发现模型只会背答案，不会举一反三，建议开启。

---

## 四、 系统与计算性能参数

### 4.1 计算类型 (Compute Type)
- **`fp16`**：标准 16 位浮点数，显存占用低。
- **`bf16` (Brain Floating Point 16)**：保持了与 fp32 一致的指数位长度。
- **结论**：**能用 bf16 绝不用 fp16**。bf16 能极大地减少因计算精度导致的训练不收敛问题。

### 4.2 量化方法 (Quantization)
- **`bnb` (BitsAndBytes)**：目前最稳健的量化库。支持 4-bit 和 8-bit 加载基座。
- **QLoRA**：即 4-bit 量化加载 + LoRA。它能让你在 24GB 显存上微调 32B 模型，且精度损失极小。

---

## 五、 参数调优实战矩阵 (故障排除)

| 观察到的异常 | 参数排查优先级 | 建议调整动作 |
| :--- | :--- | :--- |
| **准确率长期卡死** | `Rank`, `Target Modules`, `Template` | 调高 r=64; 开启 all-linear; 检查模板对齐 |
| **Loss 突然变 NaN** | `Compute Type`, `Grad Norm`, `LR` | 改用 bf16; 降低 LR; 检查数据是否有脏数据 |
| **推理输出重复/死循环** | `Warmup Ratio`, `Epochs`, `Scheduler` | 增加 Warmup; 减少 Epochs 防止过拟合 |
| **完全不听指令** | `Template`, `Loss Masking` | 确认 Response 是否被正确 Masked |

## Update History
- 2026-05-13: 初次创建。根据详尽性准则，涵盖了从基础到高阶（NEFTune, LoRA+）的全量解析，旨在建立微调百科。
